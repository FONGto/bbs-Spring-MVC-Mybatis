<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
 <mapper namespace="com.fong2.dao.EmailCodeDao">
 	
 	<select id="findByEmail" parameterType="string" 
 		resultType="com.fong2.entity.EmailCode">
 		select
 			email as email,
			code as code,
			time as time
		from 
			email_code
		where
			email=#{email}
 	</select>
 	<insert id="addCode" parameterType="com.fong2.entity.EmailCode">
 		INSERT INTO 
 			email_code (email,code) 
 		VALUES (#{email},#{code})
			ON DUPLICATE KEY UPDATE code=#{code}
 	</insert>
 </mapper>